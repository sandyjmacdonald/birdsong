import { defineConfig } from 'astro/config';

export default defineConfig({
  // Your site's public address. Used for the link-preview image, which needs a full URL.
  // Change this to your Netlify address (or your own domain) before deploying.
  site: 'https://york-birdsong.netlify.app',
});
